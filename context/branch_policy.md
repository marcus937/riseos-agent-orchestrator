# Branch Policy

Base branch is `main`.

Agent work must stay on `agent-integration` unless BB/Jarvis Architect explicitly directs another branch.

Never merge automatically. Never delete, mutate, or retarget branches as part of review. Human approval remains required before merge.
